{
  "name": "vite-chrome-extension-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm dist/ -rf",
    "dev": "vite",
    "build-dev": "vue-tsc && vite build",
    "build-prod": "npm run clean && npm run build-dev && cd dist && zip -r package.zip *",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.19"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.23",
    "@types/chrome": "^0.0.262",
    "@vitejs/plugin-vue": "^5.0.4",
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vue-tsc": "^1.8.27"
  }
}
